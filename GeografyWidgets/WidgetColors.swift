import SwiftUI

enum WidgetColors {
    static let background = Color(red: 0.05, green: 0.05, blue: 0.08)
    static let cardBackground = Color(red: 0.12, green: 0.12, blue: 0.17)
    static let accent = Color(red: 0.25, green: 0.65, blue: 1.0)
    static let accentSecondary = Color(red: 0.45, green: 0.35, blue: 1.0)
    static let textPrimary = Color.white
    static let textSecondary = Color.white.opacity(0.65)
    static let textTertiary = Color.white.opacity(0.40)
    static let divider = Color.white.opacity(0.10)
}
